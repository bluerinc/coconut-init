# GitHub Personal Access Token for cloning private GAIT repository
# Required permissions: repo (Full control of private repositories)
# 
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token" (classic)
# 3. Give it a descriptive name like "GAIT DevContainer"
# 4. Select the "repo" scope
# 5. Click "Generate token"
# 6. Copy the token and paste it below

GITHUB_TOKEN=your_github_personal_access_token_here

# Optional: GitHub Package Registry token (if different from above)
# Required permissions: read:packages
# NPM_TOKEN=your_npm_token_here
